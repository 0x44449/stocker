# ============================================
# Admin Web (Next.js)
# ============================================
# 브라우저에서 API 호출하므로 호스트 기준 URL 사용
NEXT_PUBLIC_API_URL=http://localhost:28080

# ============================================
# 공통
# ============================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=stocker
POSTGRES_USER=stocker
POSTGRES_PASSWORD=stocker

# ============================================
# 외부 포트 (로컬에서 Docker 접근용)
# ============================================
EXTERNAL_POSTGRES_PORT=5433
EXTERNAL_API_PORT=28080
EXTERNAL_ANALYZER_PORT=28000
EXTERNAL_INGEST_PORT=28081
EXTERNAL_ADMIN_WEB_PORT=23000

# ============================================
# API (Spring Boot)
# ============================================
API_LOG_LEVEL=INFO
SUPABASE_URL=

# ============================================
# Ingest (Spring Boot)
# ============================================
INGEST_LOG_LEVEL=INFO
KRX_USERNAME=
KRX_PASSWORD=

# ============================================
# MinIO (Object Storage)
# ============================================
MINIO_ROOT_USER=stocker
MINIO_ROOT_PASSWORD=stocker123
EXTERNAL_MINIO_PORT=29000
EXTERNAL_MINIO_CONSOLE_PORT=29001
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=stocker-images

# ============================================
# Analyzer (FastAPI)
# ============================================
OLLAMA_HOST=host.docker.internal
OLLAMA_PORT=11434
ANALYZER_LOG_LEVEL=INFO
